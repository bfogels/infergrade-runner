target-version = "py38"
extend-exclude = ["third_party"]

[lint]
# Default rule set: pycodestyle errors + pyflakes.
select = ["E4", "E7", "E9", "F"]

[lint.per-file-ignores]
# Entry scripts adjust sys.path before importing the runner package.
"scripts/*.py" = ["E402"]
