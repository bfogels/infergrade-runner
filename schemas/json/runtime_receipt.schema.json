{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://infergrade.dev/schemas/runtime_receipt.schema.json",
  "title": "InferGrade Runtime Receipt",
  "type": "object",
  "required": [
    "receipt_version",
    "runtime_lock_id",
    "runtime_build_id",
    "runtime_family",
    "runtime_interface",
    "content_scope",
    "origin",
    "provenance_strength",
    "locked_roles",
    "execution_tree_file_count",
    "execution_tree_manifest_sha256",
    "role_files",
    "verification"
  ],
  "properties": {
    "receipt_version": { "const": "infergrade_runtime_receipt_v1" },
    "runtime_lock_id": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "runtime_build_id": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "runtime_family": { "type": "string", "minLength": 1 },
    "runtime_interface": { "type": "string", "minLength": 1 },
    "content_scope": { "enum": ["managed_package", "selected_binary_set"] },
    "origin": { "type": "string", "minLength": 1 },
    "maturity": { "type": ["string", "null"] },
    "provenance_strength": {
      "enum": ["independently_signed", "checksum_verified", "local_fingerprint_only"]
    },
    "locked_roles": {
      "type": "array",
      "items": { "enum": ["cli", "server", "perplexity"] },
      "minItems": 2,
      "uniqueItems": true
    },
    "execution_tree_file_count": { "type": "integer", "minimum": 1 },
    "execution_tree_manifest_sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "role_files": {
      "type": "array",
      "minItems": 2,
      "items": {
        "type": "object",
        "required": ["relative_path", "kind", "mode", "size_bytes", "sha256", "roles"],
        "properties": {
          "relative_path": { "type": "string", "minLength": 1 },
          "kind": { "const": "regular" },
          "mode": { "type": "integer", "minimum": 0, "maximum": 511 },
          "size_bytes": { "type": "integer", "minimum": 0 },
          "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
          "roles": {
            "type": "array",
            "items": { "enum": ["cli", "server", "perplexity"] },
            "minItems": 1,
            "uniqueItems": true
          }
        },
        "additionalProperties": false
      }
    },
    "files": {
      "description": "Complete path-free execution-tree manifest. Present in the bundle receipt artifact and omitted from compact per-result projections.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["relative_path", "kind", "mode", "size_bytes", "sha256", "roles"],
        "properties": {
          "relative_path": { "type": "string", "minLength": 1 },
          "kind": { "const": "regular" },
          "mode": { "type": "integer", "minimum": 0, "maximum": 511 },
          "size_bytes": { "type": "integer", "minimum": 0 },
          "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
          "roles": {
            "type": "array",
            "items": { "enum": ["cli", "server", "perplexity"] },
            "uniqueItems": true
          }
        },
        "additionalProperties": false
      }
    },
    "verification": {
      "type": "object",
      "required": ["prelaunch", "postrun", "silent_substitution_allowed"],
      "properties": {
        "prelaunch": { "const": "passed" },
        "postrun": { "const": "passed" },
        "silent_substitution_allowed": { "const": false }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
